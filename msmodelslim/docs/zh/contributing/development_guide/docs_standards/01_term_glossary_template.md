# {{ term_name }} 量化术语百科词条

> **注释：**
> 本文件是「量化术语百科」的**词条模板**，用于指导开发者在 msModelSlim 知识库中新增/补全术语百科词条。
> `{{ placeholder }}` 为待填充占位符，`[OPTIONAL]` 章节按需保留。模板内说明以 `> **注释：**` 标注，正式词条中需删除。
> 模板中的示例（如链接 `《XX…》`、路径 `xxx.md`、配置片段等）均为占位示例，链接无效属正常现象，正式词条中须替换为实际文档标题与相对路径。
> 填写后须按[《量化术语百科校验清单》](01_term_glossary_checklist.md)逐项检查低错。
>
> **模板灵活性说明：** 模板章节分为「通用必填」「按需（[OPTIONAL]）」两类：
> - 「通用必填」章节（概述、词条介绍）所有词条须保留；
> - 「按需（[OPTIONAL]）」章节（关联流程、关联词条、参考文档）按需保留。
>
> **注意：词条可依据实际情况省略无关章节，不必为了对齐模板结构而填充无关内容。**

---

>
> **词条类别**：{{ term_category }}<br>
> **英文名称**：{{ english_name }}<br>
> **应用领域**：{{ application_domain }}<br>
> **首次提出**：{{ first_proposed }}
>
> **注释：** 上述内容为必填字段，用于快速定位词条的基本属性，各字段填写要求如下：
> - 词条类别：取值须在模板允许的枚举集合内（量化基础概念 / 量化算法 / 离群值抑制算法 / 敏感层分析算法 / 自动调优策略 / 量化数据格式 / 量化参数 / 量化配置项 / 量化工具接口 / 量化评估指标 / 其他）。
> - 英文名称：与文件名 `term_<english_name>.md` 中的 `english_name` 保持一致（大小写可不一致，拼写必须一致）。
> - 应用领域：简要说明词条适用的领域。
> - 首次提出：仅在有明确出处（作者、会议/期刊、年份）时填写；无明确出处则删除该行。
> - 字段校验见[《量化术语百科校验清单》](01_term_glossary_checklist.md)条目 SE-03、SE-04、SW-02、CW-06。

## 1. 概述

{{ overview }}

> **注释：** 概述要求：
> 1. 用一段话完成定义，字数控制在适度范围，精炼准确即可。
> 2. 应涵盖三个核心要素：是什么 / 解决什么问题 / 核心特征。
> 3. 行文中提及的、本身已存在或将来会存在的术语百科词条，应以超链接形式呈现：'[词条名](./term_xxx.md)'。词条链接锚文本用词条名、不加《书名号》（一级标题不含《）；正文中单独提及文档标题时才按《[公共校验清单](00_common_checklist.md)》CE-05 加《书名号》。
> 4. 避免展开算法细节或复述完整公式。
> 5. 概述中提及的相关术语应尽量以超链接形式呈现，数量结合实际分析、越多越好（覆盖正文中所有已存在或将来会存在的术语词条），以利于知识图谱构建。

## 2. 词条介绍

{{ term_introduction }}

> **注释：** 简要介绍术语的背景、解决的问题与适用定位，可包含简要历史脉络。若有需要，不必将介绍的内容作为本节的子内容，可以自由增加章节，但新增章节以及后续章节编号需要得到正确调整。
> 编写建议：
> - 可参考知识库中已有的同类词条写法，保持风格与粒度一致；
> - 也可从第一性原理出发、站在用户视角介绍：该术语是什么、为什么值得了解、与已有概念的关系。

## 3. [OPTIONAL] 关联流程

{{ related_processes }}

> **注释：** 关联流程列表要求：
> 1. 列出与本术语相关的“流程指南”类文档（如《一键量化 V1》《敏感层分析使用指南》《自动调优使用指南》等）。
> 2. 每项必须以超链接形式指向实际流程文档，禁止只写名称。
> 3. 简要说明流程与本术语的关系（如“使用本算法作为前置处理”“通过本流程可触发该术语对应的算法”）。
> 4. 若无关联流程，可整体省略本章节。
>
> 示例：
> - [《一键量化 V1》](../../../user_guide/usage_quick_quantization.md)：默认集成本算法作为离群值抑制前置步骤。
> - [《敏感层分析使用指南》](../../../user_guide/usage_sensitive_layer_wise_analysis.md)：分析结果可用于回退使用本算法的层。

## 4. [OPTIONAL] 关联词条

{{ related_terms }}

> **注释：** 关联词条列表要求：
> 1. 须使用超链接形式指向其他术语百科词条：[词条名](./term_xxx.md)。
> 2. 避免纯文本词条名（应确保可点击跳转）。
> 3. 关联词条的数量应结合实际分析、越多越好（关联词条越丰富，知识图谱连通性越强），在覆盖必要关联关系的基础上，尽量覆盖上位 / 下位 / 同类 / 对比 / 前置 / 后续 / 配套 / 应用对象 / 评估指标等各维度关系。
> 4. 每个关联词条后建议用括号或冒号简要说明关系类型，关系类型包括但不限于：
>    上位概念 / 下位概念 / 同类算法 / 对比算法 / 前置术语 / 后续术语 / 配套术语 / 应用对象 / 评估指标。
> 5. 本节中如有引用本词条正文中未解释的术语，亦应使用超链接。
> 6. 词条链接锚文本用词条名、不加《书名号》（与词条一级标题一致）；正文中单独提及文档标题时才按《[公共校验清单](00_common_checklist.md)》CE-05 加《书名号》。

## 5. [OPTIONAL] 参考文档

{{ references }}

> **注释：** 参考文档要求：
> 1. 学术论文：标题、作者、会议/期刊、年份、链接（arXiv 或官方）。
> 2. 工具内部文档：标题与相对路径链接。
> 3. 参考文档数量结合实际分析、越多越好：优先补充经典论文与权威规范的官方文档，尽可能覆盖原始出处、工具内实现与延伸阅读。
> 4. 多条以编号列表形式列出。
>
> 示例：
> 1. Xiao G, et al. "SmoothQuant: Accurate and Efficient Post-Training Quantization for Large Language Models." arXiv preprint arXiv:2211.10438, 2022. https://arxiv.org/abs/2211.10438
