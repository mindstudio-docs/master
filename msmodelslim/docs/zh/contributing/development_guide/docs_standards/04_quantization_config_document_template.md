# {{ config_name }} 配置说明

> **注释：** 将 config_name 替换为配置名称。所有 {{ placeholder_name }} 占位符必须替换为实际内容；占位符名称只能使用英文小写字母、数字和下划线，多个单词以下划线连接。发布文档前删除所有填写说明，并从保留的可选章节标题中删除 [OPTIONAL] 前缀。

## 1. 配置概述

> **注释：** 说明该配置控制的量化阶段、适用模型或场景，以及它在完整量化配置中的位置。

{{ config_summary }}

> **注释：** 填写定义该配置的源码链接及对应类名。源码链接应指向目标发布版本、标签或提交号对应的稳定仓库文件地址；有多个入口时逐项列出。

| 项目 | 内容 |
|------|------|
| 配置类 | `{{ config_class_name }}` |
| 源码 | [{{ source_file_name }}]({{ source_file_link }}) |

## 2. 参数列表

> **注释：** 列出该配置直接定义及通过继承对用户开放的全部字段。字段路径使用**相对当前配置的字段名**，不叠加整条 YAML 路径；配置在 YAML 中的位置由「配置概述」或块标题给出。类型使用 string、int、float、bool、object、list 等明确名称。必选字段的默认值填写“无”；可选字段必须给出实际默认值。取值范围或格式应包含枚举值、数值边界、路径格式和组合约束。对象或列表由其他配置定义时，在“引用配置”列链接到对应文档或页内锚点。

| 字段路径 | 类型 | 必选/可选 | 默认值 | 取值范围或格式 | 含义 | 引用配置 |
|----------|------|-----------|--------|----------------|------|----------|
| `{{ required_field_path }}` | `{{ required_field_type }}` | 必选 | 无 | {{ required_field_constraint }} | {{ required_field_description }} | {{ required_field_reference }} |
| `{{ optional_field_path }}` | `{{ optional_field_type }}` | 可选 | `{{ optional_field_default }}` | {{ optional_field_constraint }} | {{ optional_field_description }} | {{ optional_field_reference }} |

> **注释：** 参数列表按配置类名分块组织，不再单设独立的「配置约束」「引用的配置」「被引用的配置」章节：
>
> - 根配置块：`<h3 id="2-1-...">2.1 类名</h3>`，下接参数表与「配置约束」子项。
> - 嵌套配置（含任务配置的 spec）展开进本页对应类名子块，使用 `<h3>`/`<h4>` 标题（标题带可见编号，如 `2.2 类名`）；若类 docstring 首段非空，在标题后、参数表前输出一句类概述。
> - `type`/`mode` 分派字段渲染为基础类块 `<h3 id="...">2.x 基础类名（按 type 分派）</h3>`：包含基础类参数表（如基础类本身是真实配置时）、「派生类」列表（类名、判别值、一句说明、本页锚点或独立页链接），各派生类参数表与「配置约束」作为 `<h4>` 子块排在其后。
> - 同一页面同一基础类只渲染一次，后续分派字段的「引用配置」列指向该基础类块锚点。
> - 页内跳转使用 HTML 锚点 `<a href="#anchor">§x.y</a>`，不使用 Markdown 的 `{#anchor}` 属性语法。
> - 每个配置块的「配置约束」只列出能由 YAML 解析器、配置模型、校验器或加载逻辑判断的规则（互斥、依赖、优先级、继承、覆盖、回退等）；没有额外约束时填写“- 无。”。性能、精度、资源、数据代表性等运行时实践问题写入「注意事项」。

## 3. 完整配置参考

> **注释：** 提供一份语法正确、可直接复制并仅需替换业务路径即可使用的完整典型 YAML。示例必须将当前配置放在实际的完整字段路径中：顶层任务配置应包含 apiversion 和 spec；嵌套或被引用配置应放入 spec.process[]、spec.save[] 等真实引用位置，不得将子配置字段错误地直接放在 spec 下。示例必须包含本配置的必选字段和关键典型可选字段；对于复杂嵌套配置，只展开当前文档负责的配置层级和必要的一层子配置，通过 type 或辨识字段标明子配置类型，并链接到“子配置索引”或对应配置文档。不得使用省略号代替 YAML。示例值必须满足参数约束，且与参数列表、引用关系保持一致。无法提供可被配置加载入口成功解析的完整示例时，本章节可省略。

```yaml
{{ complete_config_yaml }}
```

> **注释：** 解释完整示例采用的量化场景、需要由用户替换的值及执行入口。

{{ complete_config_description }}

## 4. [OPTIONAL] 兼容性与迁移

> **注释：** 仅当配置存在版本差异、废弃字段、旧字段映射或迁移期兼容行为时保留本节。说明受影响版本、替代字段、优先级和移除计划。

| 旧字段或行为 | 新字段或行为 | 受影响版本 | 迁移方式 |
|--------------|--------------|------------|----------|
| `{{ deprecated_field_name }}` | `{{ replacement_field_name }}` | {{ affected_version }} | {{ migration_instruction }} |

## 5. [OPTIONAL] 注意事项

> **注释：** 仅当存在无法由 YAML 解析器、配置模型、校验器或加载逻辑直接判断，但会影响性能、精度、显存、设备、数据代表性、安全或运行稳定性的实践问题时保留本节。字段合法性、互斥依赖、默认/覆盖/回退等规则写入各配置块的「配置约束」。

- {{ config_notice }}
